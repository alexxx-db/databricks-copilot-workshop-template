# Step NN — <step name>  (Run <A|B>)

- **Date / operator:**
- **Type:** <A app-generated | B template | C file-drop>
- **Schema:** `<catalog>.<schema>`
- **BYO file (if any):** `<byo_glossary_file>`

## Prompt used (verbatim)

> Paste the exact text pasted into Genie Code. For Step 0, this is the **app-generated** PRD prompt
> (saved separately); for Steps 1–11 it is the static template with variables substituted.

```
<prompt text>
```

## Genie Code transcript

```
<paste the transcript: the interview, the plan, the review-then-build beat, the artifact>
```

## Artifact(s) produced

- Files written / assets created (path, MV name, space id, dashboard id, etc.):

## `.vibecoding-state.md` delta

```diff
<paste the diff of .vibecoding-state.md before → after this step>
```

## Gate result

- [ ] Gate condition met (quote the step's Gate line):
- [ ] No forbidden action (e.g. no MV YAML in Step 3):
- [ ] **Unprompted?** For Step 3: was any conflict/measure discovered that was NOT in the prompt? (Y/N + which)

## Notes / prompt edits needed

> If the step didn't reliably trigger the interview / produce its artifact / pass its gate, record
> the wording change to fold into Phase 2.
